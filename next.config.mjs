/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['dimanco.net', 'res.cloudinary.com'], // Agregar el dominio de las imágenes externas
    },
  };
  
  export default nextConfig;
  